PORT=5000
CLIENT_ORIGIN=*
DB_PATH=./data/marginline.db

# Set to "true" only for a public demo deployment. Seeds fictional sample
# data on first boot (only if the database is empty) and shows a persistent
# "DEMO MODE" banner in the UI. Never enable this against a database that
# contains, or will contain, your real financial data.
DEMO_MODE=false
