import PageEmptyState from "../components/PageEmptyState.jsx";

export const Settings = () => (
  <PageEmptyState
    title="Settings"
    description="Currency, categories, notification preferences, and data management."
    note="Arrives alongside the features it configures."
  />
);
